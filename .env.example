# ==========================================
# API 配置
# ==========================================

# 阿里云 API 密钥
ALIBABA_API_KEY=your_alibaba_api_key_here

# API 端点（海外/国内）
ALIBABA_API_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
# ALIBABA_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# OpenAI API（可选）
OPENAI_API_KEY=your_openai_api_key_here

# ==========================================
# 浏览器配置
# ==========================================

# 浏览器模式: "launch" 或 "connect"
# - launch: 启动新的 Chromium 实例
# - connect: 连接到已有的 Chrome（需要先启动 Chrome 并开启远程调试）
BROWSER_MODE=connect

# 无头模式（仅在 launch 模式下有效）
HEADLESS=false

# CDP 连接地址（connect 模式下使用，可选）
# 如果不指定，会自动检测 9222, 9223, 9224 端口
CDP_URL=http://localhost:9222

# ==========================================
# Agent 配置
# ==========================================

# 模型名称
AGENT_MODEL=qwen-plus

# 温度参数（0-1，越低越确定）
AGENT_TEMPERATURE=0.1

# ==========================================
# 运行模式
# ==========================================

# 运行模式: "single" 或 "interactive"
# - single: 执行单个任务后退出
# - interactive: 交互模式，持续接收用户输入
RUN_MODE=single